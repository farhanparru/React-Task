import React from 'react'

const Comb3 = () => {
  return (
    <div>
      <h3 className="Second">React Advantage</h3>
      <ol>
        <li>Component-Based Architecture: React encourages a modular and component-based approach to UI development.</li>
        <li>Virtual DOM: React uses a virtual representation of the DOM to optimize updates. Instead of directly manipulating the real DOM,</li>
        <li>JSX: JSX is a syntax extension that allows you to write HTML-like code within your JavaScript files.</li>
        <li>Unidirectional Data Flow: React enforces a unidirectional data flow, making it clear how data and state changes propagate through the application. x    </li>
        
      </ol>
    </div>
  )
}

export default Comb3
